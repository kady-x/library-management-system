# Example environment file for docker-compose
# Copy this file to .env and update values before running docker-compose
# Do NOT commit your .env with secrets to version control please

# Spring application configuration
# In Docker Compose, the DB host should be 'mysql' (the compose service name)
# For pure local runs, the default URL falls back to '127.0.0.1' as encoded in application.properties
SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/LMS?useSSL=false&serverTimezone=UTC
SPRING_DATASOURCE_USERNAME=username
SPRING_DATASOURCE_PASSWORD=password
SPRING_JPA_HIBERNATE_DDL_AUTO=update

# Application port
APP_PORT=8080
